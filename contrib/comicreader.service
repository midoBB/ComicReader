[Unit]
Description=ComicReader
After=network.target

[Service]
Type=simple
ExecStart=%h/.local/bin/comicreader --config %h/.config/comicreader/config.yaml
Restart=on-failure
RestartSec=5

# Keep data.db next to the config
WorkingDirectory=%h/.config/comicreader

[Install]
WantedBy=default.target
